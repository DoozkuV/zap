// place files you want to import through the `$lib` alias in this folder.
export type MenuItemProp = {
    category: string;
    description: string;
    id: number;
    name: string;
    price: number;
}